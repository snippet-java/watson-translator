declared-services:
  language-translator-service:
    label: language_translator
    plan: lite
applications:
- services:
  - language-translator-t4
  name: language-translator-demo
  command: npm start
  path: .
  memory: 256M
